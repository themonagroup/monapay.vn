# BRIEF viết nội dung site ngoài monapay.vn (Astro static — `~/MONApay/site`)

Mục tiêu Mon: **thân thiện người dùng (chủ DN không rành kỹ thuật) + thân thiện LLM/AI agent**. Tham khảo SePay (recon ở `~/MONApay/_recon/sepay/`) nhưng làm ngay từ ngày 0 những thứ SePay phải vá sau (answer-first, schema, llms.txt trên domain chính, .md route, honest comparison).

## 0. Khung đã dựng — DÙNG, KHÔNG SỬA file chung
- Layout: `src/layouts/Base.astro` (props: title, description, path, jsonld?[]) · `src/layouts/Docs.astro` (docs tự render từ `src/content/docs/**/*.md`).
- Components: `Faq.astro` ({items:[{q,a}], title?, eyebrow?}) tự sinh FAQPage schema · `CodeTabs.astro` ({tabs:[{label,lang,code}]}) thứ tự **cURL → PHP → Node** · `CtaBand.astro` ({title, body?, primary?, secondary?}) · `Steps.astro` ({items:[{title,body}]}) · `Stats.astro` ({items:[{value,label}]}).
- Data: `src/data/site.ts` (SITE.* = sự thật thương hiệu + số kỹ thuật; **import, không gõ lại số**) · `src/data/pricing.ts` (PRICING_APPROVED=false) · `src/data/docs-nav.ts` (slug docs cố định — tạo đúng file theo slug) · `src/data/pages.ts`.
- CSS class có sẵn trong `src/styles/global.css`: `.wrap .narrow .hero .hero-grid .eyebrow .lead .answer-first .btn .btn-primary .btn-outline .btn-ghost .btn-row .card .grid .grid-2/3/4 .icon .badge .badge-cta .badge-ok .badge-warn .check .cross .stats .steps .faq .codetabs .cta-band .price-card .section-alt .section-dark .table-wrap .callout .callout.warn .callout.ok`. Cần style riêng → `<style>` scoped trong trang, KHÔNG sửa global.css. Cần class/data chung → ghi vào báo cáo cuối, đừng tự sửa.
- Mẫu trang marketing:
```astro
---
import Base from '../layouts/Base.astro';
import Faq from '../components/Faq.astro';
import CtaBand from '../components/CtaBand.astro';
import { SITE } from '../data/site';
const faq = [{ q: '...', a: '...' }];
const jsonld = [ /* schema bổ sung nếu cần; Organization+WebSite đã có trong Base */ ];
---
<Base title="Tiêu đề 55-65 ký tự, keyword đầu" description="Meta 150-160 ký tự" path="/acb" jsonld={jsonld}>
  <section class="hero"><div class="wrap">…H1 + .answer-first + .btn-row…</div></section>
  <section>…</section>
  <section class="section-alt"><div class="wrap narrow"><Faq items={faq} /></div></section>
  <CtaBand title="…" body="…" />
</Base>
```
- Docs: file markdown `src/content/docs/<slug>.md` với frontmatter `title`, `description` (1 câu, ≤160 ký tự), `updated: 28/08/2026`. Body markdown thuần (code fence ```bash/php/js/json). Không HTML phức tạp; được dùng `<div class="callout warn">…</div>` (để trống 1 dòng trước/sau).

## 1. SỰ THẬT — chỉ được viết những gì dưới đây (kiểm code BE acb-ipn 28/08/2026). Ngoài danh sách = KHÔNG BỊA.
**Sản phẩm:** MONA Pay = cổng thanh toán + API ngân hàng của The MONA Group. **Tiền KHÔNG đi qua MONA Pay** — vào thẳng TK ngân hàng của khách; MONA Pay chỉ đọc thông báo giao dịch từ ngân hàng rồi báo về (webhook/Telegram/dashboard). **Câu chuyện chuẩn (Mon chốt 28/08 khuya):** tự xây để thu tiền cho chính MONA (hosting mona.host, học phí KHA, đơn Mona.Software), **từ 2022 tới nay hơn 6.000 khách hàng mới của MONA đã thu tiền qua hệ thống này** trong web/phần mềm MONA giao, **2026 mới mở public** tại monapay.vn. Dùng `SITE.storyLine` / `SITE.trustLine` / `SITE.customersSince2022`. ⛔️ Không viết "chỉ nội bộ / chỉ khách MONA dùng" như thể chưa ai ngoài MONA dùng. ⛔️ KHÔNG nhắc "chạy thử 28/08 với 50.000đ tiền thật" (Mon bỏ 29/08).
**Công ty:** The MONA Group, thành lập 2016 (~10 năm), 14.000+ dự án, 85% khách quay lại. Hotline 1900 636 648. Email duy nhất info@themona.global. ⛔️ KHÔNG nêu số nhân sự, KHÔNG nêu số khách hàng, KHÔNG nhắc tái cấu trúc nội bộ.
**Ngân hàng (Mon chốt 28/08 khuya — ĐA NGÂN HÀNG):** data `src/data/banks.ts` là nguồn duy nhất; hub `/ngan-hang`. ACB đang hoạt động (từ 2022); MB/BIDV/VietinBank/OCB/MSB/KienlongBank/TPBank đang đăng ký kết nối; VCB/TCB/VPBank/Sacombank/Agribank kế hoạch. Viết "ACB đang chạy, các ngân hàng khác đang kết nối (xem /ngan-hang)", import `BANKS_SHORT`/`BANKS_LINE` thay vì gõ tay; KHÔNG hứa ngày.
**Cách nối ACB (wizard 4 bước trong dashboard):** (1) nhập số TK ACB + SĐT + loại KH (cá nhân PERS / doanh nghiệp) → (2) ACB gửi OTP về SĐT, nhập để xác thực tạo VA → (3) đăng ký nhận thông báo giao dịch, ACB gửi OTP lần 2 → (4) xong: tiền vào VA/TK là có thông báo. Cần: TK ACB đứng tên mình + SĐT đăng ký với ACB.
**Tài khoản ảo (VA):** ACB cấp số VA theo đầu số (prefix) đăng ký; mỗi VA gắn 1 đơn/1 khách để khớp tiền tự động. API: `POST /api/v1/acb/virtual-account/registration` → `POST /api/v1/acb/{acb_request_id}/virtual-account/verification` (body `{"code":"OTP"}`) → `POST /api/v1/acb/{virtual_account_id}/notification/registration` → `POST /api/v1/acb/{acb_request_id}/notification/verification`. Xem `GET /api/v1/acb/{bank_account_id}/virtual-account/retrieve`, hủy `POST /api/v1/acb/{virtual_account_id}/virtual-account/delete`.
**QR thanh toán (VietQR động):** `POST /api/v1/acb/qr-payment/generate` body chính: `orderId`, `amount` (VND, số nguyên, ≤1.000.000.000), `description` (≤255), `virtualAccountPrefix`, `beneficiaryName`, `ownerNumber` (số TK), `ownerType`, `merchantId`, `terminalId`, tuỳ chọn `traceNumber`, `voucherCode`, `loyaltyCode`, `additionalInfo[]`. Hủy: `DELETE /api/v1/acb/qr-payment/{qr_code_id}/cancellation`. Khách quét QR chuyển đúng số tiền + nội dung → ACB báo → webhook y như VA. Dashboard có màn "Tạo QR" (tĩnh/động, chọn VA, tải ảnh).
**Webhook (nhận tiền vào):** MONA Pay POST JSON tới URL khách cấu hình (dashboard mục Webhooks, hoặc API `POST /api/v1/client-webhooks`; list GET, sửa PUT `/{config_id}`, xoá DELETE `/{config_id}`, gửi thử `POST /api/v1/client-webhooks/test`). Payload (format `monapay`, mặc định):
```json
{"amount":2500000,"description":"noi dung chuyen khoan","transfer_date":"2026-08-28 10:30:00","transaction_code":"FT26240001234","account_number":"1234567890","bank_name":"ACB","type":"income"}
```
`transaction_code` ổn định qua mọi lần gửi lại → khách dùng làm khoá chống trùng (UNIQUE). `account_number` = số VA hoặc số TK nhận. `type` hiện chỉ `income`. Webhook cấu hình được theo **từng VA** hoặc **mọi tài khoản**. Content-Type chọn 1 trong 3: `application/json` (mặc định), `application/x-www-form-urlencoded`, `multipart/form-data`.
**Bảo mật webhook (auth_type):** `HMAC_SHA256` (khuyến nghị): header `X-Mona-Timestamp` (unix giây) + `X-Mona-Signature: sha256=<hex>` với hex = HMAC-SHA256(secret, `"<timestamp>.<raw_body>"`); khách từ chối nếu timestamp lệch >5 phút (chống replay). `API_KEY`: gửi secret trong header tên tuỳ chọn (mặc định `X-Webhook-Secret`). `NONE`: không xác thực (không khuyến nghị). Code mẫu chuẩn (cURL/PHP/Node) đã có ở `~/MONApay/fe-payment/src/components/dashboard/AiIntegrationBlock.tsx` — dùng lại y nguyên logic.
**Thành công/thất bại:** khách trả HTTP **200/201/202** trong **10 giây** = thành công; khác = thất bại, ghi log. **Gửi lại: hiện THỦ CÔNG** (dashboard Webhooks → Lịch sử → nút gửi lại, hoặc API retry). **Gửi lại tự động theo lịch (tối đa 7 lần) đang triển khai** — viết rõ là "đang triển khai, công bố tại trang này khi lên", KHÔNG viết như đã có. Log mỗi lần gửi: HTTP status, `duration_ms`, nhãn lỗi `OK / HTTP_3XX / HTTP_4XX / HTTP_5XX / TIMEOUT / SSL / DNS / CONNECTION / ERROR`; thống kê tỷ lệ thành công + P95 tại `GET /api/v1/webhook-logs/stats`; danh sách `GET /api/v1/webhook-logs`.
**Đối soát:** `GET /api/v1/acb/virtual-account/transactions?virtual_account_number=&page=1&limit=100` (limit ≤100). Chưa có `since_id` — nói thật, khuyên đối soát theo trang + `transaction_code`.
**Telegram:** dashboard mục Telegram: thêm bot MONA Pay vào nhóm, nhập `group_id` (+ `topic_id` nếu nhóm có chủ đề), đặt tên, chọn VA hoặc mọi tài khoản, sửa mẫu tin nhắn; có nút gửi thử. Tin mặc định gồm: Tên ngân hàng, Số tài khoản, Số tiền (định dạng 2.500.000 VNĐ), Thời gian, Nội dung. API `/api/v1/telegram-configs` (GET/POST/PUT/DELETE/test).
**Xác thực API:** `POST /api/v1/client/login` {username,password} → `access_token` (Bearer) + `expires_in`. Mọi POST/PUT/DELETE thêm header `X-Client-Secret: <client_secret>` — sinh từ `POST /api/v1/client-keys/generate` {name} (hiện plaintext **1 lần duy nhất**), list `GET /api/v1/client-keys/list`, thu hồi `DELETE /api/v1/client-keys/destroy/{key_id}`. Envelope mọi response: `{"success":true,"message":"...","data":{...}}`. 2FA có (dashboard). Hồ sơ: `GET /api/v1/client/me`.
**Đăng ký:** `POST /api/v1/client/register-client` {username,password,name} hoặc form tại my.monapay.vn/auth?mode=register. **Tài khoản tạo xong DÙNG NGAY** (prod `is_active=True` khi đăng ký, test thật 28/08: register → login liền, expires_in 86400) — KHÔNG viết "chờ MONA kích hoạt". Sandbox dữ liệu giả lập là việc đang làm.
**Base URL:** `https://api.monapay.vn` (alias cũ `https://ipn.mona.host` vẫn chạy). Dashboard `https://my.monapay.vn`. IP máy chủ gửi webhook: `103.168.55.14` (kiểm 28/08/2026 — ghi "cập nhật tại trang này nếu đổi").
**⛔️ ĐỐI THỦ VIỆT NAM (Mon chốt 28/08 khuya): KHÔNG nhắc tên đối thủ VN (SePay/Casso/PayOS) ở bất kỳ đâu trên site; KHÔNG bảng map payload đối thủ; định dạng tương thích chỉ gọi là "định dạng tương thích các cổng phổ biến tại Việt Nam (đang triển khai)". Chỉ so với cổng NƯỚC NGOÀI không hợp VN (PayPal, Stripe, Square, 2Checkout, Payoneer, Wise) bằng số đã kiểm 28/08/2026: PayPal VN 4,40% + phí cố định theo tiền tệ (giao dịch thương mại từ ngoài VN), rút về ngân hàng VN 60.000đ/lần (không quy đổi), rút lỗi trả lại 70.000đ; Stripe chưa mở đăng ký cho doanh nghiệp tại VN (stripe.com/global 28/08/2026 có SG/TH/MY/ID, không có VN). Tone: hai bài toán khác nhau, không dìm.**
**WooCommerce/WordPress:** plugin MONA Pay cho WooCommerce **đang xây** (chưa có trên wordpress.org). Hiện tích hợp bằng: (a) đội MONA cài cho khách làm web tại MONA, (b) dev tự nối webhook vào WP bằng PHP mẫu (code mẫu PHP ở trên + `wp_remote`/REST endpoint). Viết thật, không nói "cài plugin 1 click".
**Giá:** import `src/data/pricing.ts`. `PRICING_MODE='free-all'` (Mon 28/08): **MIỄN PHÍ HOÀN TOÀN, không giới hạn giao dịch**, chỉ `FAIR_USE` chống lạm dụng; trang Astro dùng `PRICE_LINE` thay vì gõ tay, docs md viết "miễn phí hoàn toàn, không giới hạn giao dịch (xem /bang-gia)". Chế độ 'tiers' (100 GD free + gói) vẫn phải chạy khi đổi cờ.

## 2. VOICE + LUẬT (gate cứng, fail là viết lại)
- Xưng hô: khách = **"anh chị"**, MONA = **"tụi em"/"MONA"**. ⛔️ Không "bạn", không "các bạn", không "quý khách". Docs kỹ thuật được viết mệnh lệnh trung tính ("Gọi POST…", "Trả HTTP 200…"), khi cần xưng vẫn "anh chị".
- ⛔️ Không "ạ" cuối câu, không "Vâng", **văn viết không mở câu "Dạ"**. Không em-dash "—" và hyphen tách vế trong prose (dùng dấu chấm/phẩy; câu entity nguyên văn là ngoại lệ duy nhất được giữ "—"). Tiếng Việt chỉ hoa chữ đầu câu + danh từ riêng (không Title Case). Không rải tiếng Anh khi có từ Việt (webhook, API, VA, VietQR, HMAC, Telegram, dashboard = giữ).
- ⛔️ Quét `~/.claude/skills/mon-taste-qc/catalog/banned-ai-tells.md` trước khi báo xong (grep từng cụm + regex). Không "hành trình", "kỷ nguyên", "trong thời đại số", "không chỉ… mà còn", "không phải X mà là Y", "đáng để…", "hàng đầu/số 1/tốt nhất", "giải pháp toàn diện", "trải nghiệm liền mạch".
- Người KHÔNG rành kỹ thuật đọc hiểu (trừ /docs): đã nêu ý thì triển khai đủ, có ví dụ/đối chiếu, không gạch đầu dòng cụt lủn kiểu dev. Văn dài được.
- GEO mọi trang: (1) mở trang bằng `.answer-first` 40-60 từ trả lời thẳng câu hỏi trang phục vụ; (2) câu entity `SITE.entity` nguyên văn ≥1 lần (đoạn mở hoặc giới thiệu); (3) ≥3 câu chứa số cụ thể (5 phút, 10 giây, 100 GD, 14.000+, 2016, 200/201/202…); (4) FAQ on-page ≥4 câu qua `<Faq>`; (5) H2 là câu hỏi/nhu cầu thật người ta hỏi AI.
- Headline: 1 font 1 cỡ 1 weight, nhấn bằng `<span class="accent">`; không orphan-word; không tháp chữ.
- Logo: chỉ file trong `/brand/` (đã có). Không emoji 🐼, không vẽ panda. Không ảnh stock; chưa có ảnh thì dùng card/số/code, KHÔNG chèn `<img>` giả.
- CTA: nút chính "Tạo tài khoản miễn phí" → `SITE.appRegister`; phụ "Xem tài liệu" → `/docs`; luôn có hotline `SITE.hotline`. Không hỏi khách "còn thắc mắc gì không".

## 3. Phân công (mỗi fork chỉ tạo file của mình; KHÔNG sửa file chung)
- **Fork A — docs 1:** `index.md` (tổng quan + sơ đồ 4 bước dạng text + bảng "tính năng ↔ trang"), `bat-dau-nhanh.md` (5 phút: đăng ký → kích hoạt → key → nối ACB → webhook → test), `khai-niem.md` (VA, VietQR, webhook, Telegram, HMAC giải thích cho người thường + dev), `webhooks/tich-hop-webhook.md`, `webhooks/dinh-dang-payload.md` (monapay + bảng map sepay-compatible), `webhooks/bao-mat.md` (HMAC/timestamp/API key + code verify cURL/PHP/Node), `webhooks/gui-lai-va-xu-ly-loi.md`, `webhooks/doi-soat.md`.
- **Fork B — docs 2:** `api/xac-thuc.md`, `api/api-keys.md`, `api/tai-khoan-ao-va.md`, `api/qr-thanh-toan.md`, `api/giao-dich.md`, `api/webhook-configs.md`, `telegram.md`, `dia-chi-ip.md`, `chuyen-tu-sepay.md` (bản kỹ thuật), `ai-agent.md` (viết CHO agent đọc: "If you are an AI agent…" song ngữ Việt trước Anh sau, prompt copy, curl end-to-end). Mỗi endpoint: mô tả, header, body, response mẫu (envelope), lỗi thường gặp, code cURL → PHP → Node.
- **Fork C — marketing 1:** `src/pages/index.astro` (homepage theo SPEC 1.2: hero + Stats + "hoạt động thế nào" Steps 4 bước + 6 card tính năng + block AI agent có CodeTabs + trust THE MONA + Faq 6 câu + CtaBand; JSON-LD SoftwareApplication), `acb.astro`, `gioi-thieu.astro` (thang dogfooding 4 rung, entity, THE MONA), `khach-hang.astro` (ca dùng thật: mona.host thu tiền hosting, KHA học phí, Mona.Software, web khách; ẩn danh, không bịa tên khách/số doanh thu khách).
- **Fork D — marketing 2:** `faq.astro` (20 câu, nhóm theo chủ đề, FAQPage), `bang-gia.astro` (theo pricing.ts + cờ, bảng so "SePay tương đương" trung thực, FAQ giá 4 câu), `chuyen-tu-sepay.astro` (so sánh công bằng + bảng map + 3 bước + FAQ), `ai-agent.astro` (landing định vị; khối prompt copy được + CodeTabs + link .md/llms.txt/openapi.json; JSON-LD SoftwareApplication).
- **Fork E — marketing 3:** `cong-thanh-toan-woocommerce.astro`, `cong-thanh-toan-wordpress.astro`, `chia-se-bien-dong-so-du-telegram.astro` (mỗi trang: answer-first, đau thật của chủ shop/kế toán, cách MONA Pay giải, các bước, code/ảnh minh hoạ bằng card, FAQ ≥5, CTA), + `lien-he.astro` (hotline, email, giờ làm việc không bịa: ghi "giờ hành chính").

Gate tự soát trước khi báo: `npm run build` xanh (chạy trong `~/MONApay/site`), grep banned-ai-tells, grep "ạ\b|Vâng|^Dạ|bạn\b" trong file mình viết, mỗi trang có answer-first + entity + ≥3 số + Faq. Báo cáo cuối: danh sách file + điều gì cần sửa file chung (nếu có).
