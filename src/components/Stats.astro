---
interface Props { items: { value: string; label: string }[] }
const { items } = Astro.props;
---
<div class="stats">
  {items.map((s) => <div class="stat"><b>{s.value}</b><span>{s.label}</span></div>)}
</div>
