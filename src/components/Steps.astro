---
interface Props { items: { title: string; body: string }[] }
const { items } = Astro.props;
---
<div class="steps">
  {items.map((s) => (
    <div class="step">
      <h3>{s.title}</h3>
      <div set:html={s.body.startsWith('<') ? s.body : `<p>${s.body}</p>`} />
    </div>
  ))}
</div>
