# BRIEF — 10 trang "Tạo mã QR <ngân hàng>" dùng chung tool (Fork Q)

Mục tiêu SEO: cụm `tạo mã qr [bank]` 60–350/biến thể (Ahrefs 27/08): Vietcombank, Techcombank, MB Bank, BIDV, VietinBank, Agribank, VPBank, TPBank, Sacombank, ACB. Tool đã có tại `src/pages/tao-ma-qr-ngan-hang.astro` (+ `/js/vietqr-core.js`, `BANKS` có BIN). Làm 1 route động `src/pages/tao-ma-qr/[bank].astro` + data `src/data/qr-banks.ts` (slug, tên, BIN, 2–3 dòng đặc thù ĐÚNG SỰ THẬT chung: tên đầy đủ ngân hàng, app chính thức tên gì (VCB Digibank, Techcombank Mobile, MB Bank/App MBBank, SmartBanking, VietinBank iPay, Agribank Plus/E-Mobile Banking, VPBank NEO, TPBank Mobile, Sacombank Pay, ACB ONE) — không bịa số khách/chi nhánh/phí của bank).
Mỗi trang: title "Tạo mã QR <Bank> (VietQR) miễn phí, quét là chuyển đúng tiền" (≤65), H1 keyword-led ≤2 hàng, answer-first 40–60 từ, **tool nhúng y hệt trang gốc nhưng bank đã chọn sẵn và khoá/ưu tiên** (tái dùng markup + script: tách phần form/script tool thành component `src/components/QrTool.astro` với prop `bin`/`bankName` để cả trang gốc lẫn 10 trang dùng chung — được sửa trang gốc để import component, giữ nguyên hành vi đã test), 3 H2 nội dung riêng (cách khách quét bằng app của bank đó — chỉ nói "mở app <tên app>, chọn quét QR", không mô tả nút bấm chi tiết; QR tĩnh vs động cho shop; sau khi quét ai xác nhận → MONA Pay chỉ nối ACB hiện tại — với bank khác ACB nói thật: "MONA Pay đang làm việc để mở thêm ngân hàng, hiện tự xác nhận qua tài khoản ACB"), FAQ ≥4 riêng theo bank, CtaBand. JSON-LD WebApplication + Breadcrumb; canonical riêng từng trang; link chéo 10 trang + trang gốc; thêm 10 dòng vào `src/data/pages.ts`; thêm vào `src/data/nav.ts` KHÔNG (parent làm).
Gate: build xanh, `scripts/qc.py dist` 0 FAIL, voice/tells 0, không SePay/Casso/PayOS, test tool trên 1 trang bằng Playwright nếu browser rảnh (fill form → canvas có pixel tối, nút Tải PNG bật) — bận thì báo parent đo.
